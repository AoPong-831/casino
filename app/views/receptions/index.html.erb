<p>受信画面</p>
<table border = "1">
    <tr>
        <th>user_id</th>
        <th>money</th>
        <th>flag</th>
        <th>承認ボタン</th>
    </tr>
<% @receptions.each do |a| %>
        <tr>
            <% b = User.find_by(id: a.user_id) %>
            <th><%= b.name %></th>
            <th><%= a.money %></th>
            <% if a.flag == 1 %>
                <th><%= "引出" %></th>
            <% elsif a.flag == 2 %>
                <th><%= "預入" %></th>
            <% else %>
                <th><%= "error" %></th>
            <% end %>
            <th><%= link_to "承認","/receptions/index/#{a.id}" %></th>
        </tr>
<% end %>
</table>